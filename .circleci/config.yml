# Use the latest 2.1 version of CircleCI pipeline process engine.
# See: https://circleci.com/docs/2.0/configuration-reference
version: 2.1

  jobs:
    test-workdir-1:
      machine: true
      resource_class: circleci-runner/atul-1
      working_directory: myproject
      steps:
        - run: pwd
        - run: echo "runner dir = $CIRCLE_WORKING_DIRECTORY"
    test-workdir-2:
      machine: true
      resource_class: circleci-runner/atul-2
      steps:
        - run: pwd
        - run: echo "runner dir = $CIRCLE_WORKING_DIRECTORY"
