stages:
  - build
  - test

job_build:
  stage: build
  script:
    - echo "Building the project"
    - date >> /tmp/gitlab-build

job_test:
  stage: test
  script:
    - echo "Running tests"
    - echo "---------------" >> /tmp/gitlab-build